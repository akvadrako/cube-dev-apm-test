{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run application",
            "type": "node-terminal",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "command": "docker-compose up -d --build"
        }
    ]
}  
